{
  "name": "local-ip-api",
  "version": "1.0.0",
  "description": "Local Node API to return machine's private IPv4",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
